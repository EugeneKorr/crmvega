# 🚀 Обновленная инструкция по Staging (Без вебхука бота)

Так как ваш бот управляется через **Bubble** (вебхуки от Telegram приходят туда), а CRM только **отправляет** сообщения через API:

**Это идеально для нас!**

Вам **не нужно** ничего менять в логике бота.

## 1. Ветка Staging
Мы уже находимся в ветке `staging`.

## 2. Настройка Backend (Render)
1. Зайдите в [Render Dashboard](https://dashboard.render.com/).
2. Создайте **New Web Service**.
3. Репозиторий `crm-vega`, ветка **`staging`**.
4. Имя: `crm-backend-staging`.
5. Environment Variables (скопируйте **ВСЁ** как на Production):
   - `TELEGRAM_BOT_TOKEN`: Тот же самый боевой токен. Это безопасно, так как мы только отправляем сообщения.
   - `SUPABASE_URL`, `SUPABASE_KEY`...
   - `FRONTEND_URL`: Ваш будущий staging-фронтенд (см. пункт 3).

## 3. Настройка Frontend (Vercel)
1. Зайдите в [Vercel Dashboard](https://vercel.com).
2. Создайте **New Project**.
3. Репозиторий `crm-vega`.
4. В **Deploy Settings** выберите **Production Branch**: `staging`.
5. Environment Variables:
   - `VITE_API_URL`: Адрес вашего *нового* Render сервиса (`https://crm-backend-staging.onrender.com/api`).
   - `VITE_SOCKET_URL`: Адрес вашего *нового* Render сервиса (`https://crm-backend-staging.onrender.com`).

---

**Результат:**
У вас будет точная копия продакшена на `crm-frontend-staging.vercel.app`.
Вы сможете зайти туда под своим логином (база та же), открыть любой чат и попробовать отправить сообщение. Оно уйдет клиенту от имени того же бота.

**Риски:** Нет. Вы просто используете другой интерфейс для доступа к той же системе.
